// 一次性脚本:把"AIOS 核心能力优化计划"(调研支撑版)做成专业 PDF。跑法 npx tsx src/make-plan.ts
import { mkdirSync } from 'node:fs';
import { join } from 'node:path';
import { writeDeliverable } from './build';

const md = `# AIOS 核心能力优化计划

## —— 从底层逻辑解构四大方向 · 调研支撑 · 仅规划

**日期**:2026-06-01  **状态**:规划稿(经深度调研 + 自有横评实测支撑,**待拍板后启动**)
**调研基础**:LLM 路由方向 22 条经对抗验证的学术 claim(ICLR/ICML 2025、TMLR)+ Self-Repair/LLM-as-judge/LiteLLM 一手源 + 本团队三模型横评一手数据

---

## 〇、总纲:一条主线串起四个方向

四个优化方向看似独立,底层其实是**同一条命脉——"质量估计 / 反馈的可靠性"**:

- **路由**靠 *ex-ante*(生成前)质量预测,选对模型;
- **自愈**靠 *post-hoc*(生成后)客观反馈,修对地方;
- **评测**靠可靠的 judge,才能给路由喂准画像;
- **扩池**靠评测,才知道新模型擅长什么。

cascade routing 论文(ETH, ICLR 2025)最被反复复现的结论是「**质量估计器是模型选择范式成败的唯一关键因素**,估计噪声一大,收益就塌回基线」;Self-Repair 论文(2306.09896)的核心瓶颈也是「**模型自反馈质量不足**,要强反馈才有效」。两条独立证据链殊途同归。

**结论:AIOS 真正的技术护城河 = 可靠的质量估计 + 客观反馈体系,而不是某条路由规则、更不是某个模型。** 四个方向都要围绕"把质量估计/反馈做可靠"来设计。

AIOS 的三个结构性优势恰好都落在这条线上:① **playtest(headless 真机执行)是客观反馈**,天然强于模型自评;② **中立编排**能跨厂商给每个模型建能力画像(OpenAI/Anthropic 做不了左右手互搏);③ **BYOK 成本解耦**——用户付费,路由可以激进地"质量优先",不必像 ChatGPT 那样偷偷降级省成本。

---

## 一、方向一:路由精细化

### 1.1 底层逻辑
模型选择有两个原语:**路由**(生成前一次性选模型,基于对查询难度/类型的预判)与**级联**(先调便宜模型,用质量评分 + 阈值决定是否升级到更贵模型)。2025 年 ETH 把二者统一成**cascade routing**,在其估计器模型下被证明最优。三者共同依赖一个东西:**对"这个模型在这个查询上会表现多好"的估计**。

### 1.2 调研支撑(关键数据)
| 方法 | 关键结果 | 对 AIOS 的启示 |
|---|---|---|
| RouteLLM (ICLR'25) | 强/弱双模型路由,MT-Bench 省 85%@95% 质量;**但 MMLU 仅省 45%、GSM8K 35%**(85% 是天花板) | 收益高度依赖任务分布,不能照搬百分比 |
| 4 种路由器 | 矩阵分解(mf,推荐)/相似度加权 Elo/BERT/因果 LLM,从 Arena 偏好数据学 | 有现成菜单可借 |
| FrugalGPT (TMLR'24) | 级联:cheap-first + 评分早停,省 59–98% | "先便宜后升级"模式 |
| cascade routing (ICLR'25) | 统一路由+级联,RouterBench +8%、SWE-Bench +14%;**模型越多优势越大,估计噪声越大优势越小(真实噪声下仅 +1.2%)** | 模型池越广越值;估计器是命门 |
| InferenceDynamics (2505.16303) | **参数-free**:7 维能力画像(推理/理解/指令/agentic/知识/编码/多语言)+ argmax 加权和 + cost-penalty β 旋钮;路由 8 模型超最佳单模型 1.28 分 | **最契合 AIOS**——无需训练、加模型不用重训、自带成本旋钮 |
| GraphRouter (ICLR'25) | 图边预测,超基线 12.3%;**新模型无需重训**(+9.5%),训练时间 542s→3s | 可替换模型池的路由范式 |
| kNN 路由 (2505.12601) | 检索 k=100 相似历史查询、聚合其表现;**匹配复杂学习路由器** | **从简单起步**,别上来就训大模型 |
| RouterBench (TMLR'24) | 405k 结果/11 模型/64 任务的验证基准;成本差 2–5x(DeepSeek 时代更大) | 验证路由器的标准 |

### 1.3 AIOS 现状
\`router.ts\` 是硬编码 if-else:\`code→千问、deep→DeepSeek-V4-Pro、其余→DeepSeek-V4-flash\`。优点:简单、可解释、零延迟。缺点:静态、不自学习、加任务类型要改代码。

### 1.4 优化方案(分阶段,底层逻辑驱动)
- **阶段 1(P0,立即)**:把横评结论固化成**显式路由表**(\`任务类型 → 模型 + 理由 + 备选\`,配置化),取代散落的 if-else。这是"可解释 + 可迭代"的基线。
- **阶段 2(P1)**:仿 InferenceDynamics 做**参数-free 能力画像**——给每个模型一张"能力卡"(造物/文档/推理/多模态/速度 5–7 维评分),每个任务一份"需求向量",路由 = argmax(画像·需求) − β·成本。横评数据就是能力卡的初始值。**β 旋钮**给用户"要快还是要质量"的弹性(BYOK 下可激进偏质量)。
- **阶段 3(P2)**:**kNN 自学习路由**——每次产出记录 \`(任务embedding, 模型, 客观质量)\`,新查询检索 k 个最相似历史、投票选模型。越用越准,且天然适配新模型(无需重训)。
- **贯穿**:① 中文模型池必须**自建画像**(英文基准不可套,这是调研明确的 open question);② AIOS 产**成品**(非 QA 单答案),质量估计要从"单答案打分"升级为"成品可用性评估"(接方向三)。

### 1.5 落地细节
路由表 schema:\`{taskType, primary, fallback[], reason, costTier}\`;能力卡:\`{model: {造物:0-1, 文档:0-1, ...}}\`(横评归一化填充);β 公式:\`score = Σ(cap_i × need_i) − β × normCost\`;kNN:查询用现成 embedding(豆包/千问 embedding API 已有)+ 余弦检索 + 客观质量(来自方向三的自测)聚合。

---

## 二、方向二:可替换模型池

### 2.1 底层逻辑
"不被任一模型锁死"的工程实现 = **统一接口 + provider 自报元数据 + 多级 fallback**。模型变成可热插拔的零件,上层逻辑对"具体是谁"无感。

### 2.2 调研支撑(LiteLLM 成熟模式)
统一 \`completion()\` 接口跨厂商透明;路由策略可选(simple-shuffle/latency/cost/least-busy/自定义);**fallback 链**(按 order 优先级逐级重试,组内加权);**重试**(RateLimit 指数退避);**冷却机制**(一分钟内失败 >1 次则暂时摘除该部署,默认 5s);**上下文窗口预检**(避免超长 prompt 发小窗口模型);**成本解耦**(per-token 成本覆盖,支持 BYOK)。

### 2.3 AIOS 现状
已有干净的 \`Provider\` 抽象(\`id/label/available/chat\`)+ \`getProvider\` 兜底 DeepSeek;实测接千问/豆包/DeepSeek 各只需几处改动——**基础已经对**,且已是 OpenAI 兼容统一接口。

### 2.4 优化方案(借 LiteLLM 补齐工业级可靠性)
1. **多级 fallback 链**:现在只兜底 DeepSeek,升级为"主→备1→备2"(如千问造物失败→豆包→DeepSeek),按 provider 健康度动态。
2. **冷却 / 健康追踪**:某模型连续失败(限流/超时)则短暂跳过,健康自动恢复——避免一个模型挂了拖垮整体。
3. **上下文窗口预检**:发请求前查模型上下文上限,超长自动截断/换大窗口模型(直接缓解 DeepSeek 造物的 8192 截断问题)。
4. **Provider 元数据**:给每个 provider 加 \`meta:{contextLimit, costPerToken, capabilities}\`,直接喂方向一的路由画像。
5. **铁律**:绝不为单一模型深度定制——所有模型走同一 \`chat\` 接口,保持每个可替换。

### 2.5 落地细节
\`Provider\` 接口扩展 \`meta\` 字段;\`getProvider\` → \`routeWithFallback(taskType)\` 返回有序候选链;冷却用内存状态机(\`{model: {failCount, cooldownUntil}}\`);上下文预检在 \`agent.ts\` chat 前。

---

## 三、方向三:结果标准 + 自测闭环(下游兜底)

### 3.1 底层逻辑
模型输出不可靠(每个都有短板),所以必须有**下游验证 + 修复**。但调研给了关键警告:**self-repair 不是银弹**。

### 3.2 调研支撑(Self-Repair 论文核心发现)
- self-repair 收益往往**有限**,子集间差异大,有时**没有收益**;
- **核心瓶颈是"模型自反馈质量不足"**——靠模型自己挑错改错,能力有限;
- **强反馈才有效**:更强模型 / 真实执行结果作反馈时,收益显著;
- 即便 GPT-4,self-repair 仍**远低于人类调试**。
- (相关:Reflexion 2303.11366 / Self-Debugging 2304.05128 都靠**执行反馈**而非纯自省。)

### 3.3 AIOS 现状 + 为什么方向已经对
AIOS 已有 \`playtest.ts\`(headless 真机跑 + 检测报错/画面响应/秒结束)+ agent 内"生成→自测→反馈修复"循环。**这正是调研说的"客观执行反馈",天然强于模型自评——AIOS 在这点上方向就对了。** 但实测暴露 playtest 启发式弱:落地页(无 canvas)被误判、被自动动画骗(豆包马里奥假 PASS)。

### 3.4 优化方案
1. **强化"客观反馈"(而非加强模型自省)**——这是调研的核心指引:
   - **按产物类型分流检测**:游戏(canvas+交互)、网页(DOM+无报错)、文档(结构+完整)各用各的检测器,不再一刀切假设 canvas。
   - **检测真实交互**:对游戏,注入合成按键后读**游戏状态变化**(不是画面 hash——画面会被自动动画骗),逼近"真的能操控吗"。
   - **结果标准库**:为每类产物定义"可用"的客观判据(游戏:角色响应输入;报告:章节+数据齐;网页:零运行时报错)。
2. **修复循环用强反馈**:把客观症状(\`运行时报错 X\`、\`按键后状态不变\`)喂回模型——这是"强反馈",比让模型自评有效得多。
3. **成本-收益门**:self-repair 有成本(多轮 LLM)。简单产物多修两轮;复杂产物若两轮不收敛,**换模型**(接方向一)比死磕同一个模型更划算——这本身就是编排的价值。
4. **续写拼接改进**:DeepSeek 造物因截断+续写崩,可改进拼接(重叠锚点、语法校验);但本质上**路由千问规避**更经济(编排 > 硬修)。

### 3.5 落地细节
\`playtest\` 增加 \`kind: 'game'|'web'|'doc'\` 分流;游戏检测注入 KeyboardEvent 后读暴露的状态/或对比"无操作 vs 有操作"两段 canvas 差异(隔离自动动画);结果标准库 = \`{kind: 判据函数}\`;修复循环上限按产物复杂度动态(2–3 轮)。

---

## 四、方向四:扩模型池 + 评测体系

### 4.1 底层逻辑
模型池越广,"各家所长"的并集越大(cascade routing 已证:候选越多优势越大)。但**加模型前必须先评测**——不评测就没有能力画像,路由就是瞎猜。评测可用 LLM-judge,但它有偏见。

### 4.2 调研支撑(LLM-as-judge)
GPT-4 评委与人类 agreement **>80%**(达人类间一致性水平),可用;但有三类偏见:**position bias**(偏好某位置)、**verbosity bias**(偏好长答案)、**self-enhancement bias**(偏好自己的输出)+ 有限推理能力。

### 4.3 AIOS 现状
已有横评脚本(\`eval-models.ts\`,5 任务 × N 模型 + 客观指标 + playtest 启发式),已实测出"千问代码王/DeepSeek 文档强/豆包结构好"。

### 4.4 优化方案
1. **扩池**:接 Kimi、GLM、视觉模型(标准 provider,几处改动);多模态打开"做视频/图"的造物新边界(火山方舟 seedance/seedream 已验证可达)。
2. **评测常态化(喂路由画像)**:新模型接入即跑横评 → 自动更新方向一的能力卡。形成"接入→评测→画像→路由"闭环。
3. **指标分层**:客观指标(截断/完整/速度/体积/文档结构,可靠)为主 + playtest(启发式,参考)+ 未来 LLM-judge(开放质量)。
4. **LLM-judge 降噪**(用时必做):**位置交换**(消 position bias)、**多评委投票**、**禁止自评**(消 self-enhancement,千万别让千问评千问的代码)、控制长度(消 verbosity)。
5. **建 AIOS 自己的 benchmark**:仿 RouterBench,但用**中文 + 成品任务**(报告/游戏/网页),因为调研明确"英文 QA 基准不能代表 AIOS 的成品生成场景"。

### 4.5 落地细节
横评脚本加 \`--update-profile\` 自动写回能力卡;judge 模块:同一对产物正反位各评一次取均值 + 3 评委多数;benchmark 任务集版本化、可重跑取均值降随机噪声。

---

## 五、实施优先级(仅排序,不启动)

| 优先级 | 事项 | 价值 | 依赖 |
|---|---|---|---|
| **P0** | 路由表显式化(横评结论落地)+ Provider 元数据 | 立竿见影、零风险 | 无 |
| **P0** | 多级 fallback 链 + 冷却 + 上下文预检 | 工业级可靠性、缓解截断 | Provider 元数据 |
| **P1** | 能力画像 + cost-penalty β 旋钮 | 路由从静态→可调 | 路由表 |
| **P1** | playtest 分类检测 + 强反馈修复 | 自测真正可靠 | 结果标准库 |
| **P2** | kNN 自学习路由 | 越用越准 | 能力画像 + 自测质量数据 |
| **P2** | 评测常态化 + LLM-judge 降噪 | 画像自动更新 | 横评脚本 |
| **P3** | 扩池(Kimi/GLM/视觉)+ 中文成品 benchmark | 并集更大、覆盖多模态 | 评测体系 |

---

## 六、一句话收束

**别为任何单一模型优化 agent——那是把天花板焊死。** AIOS 的功夫全下在"编排层"上,而编排层的内核是**可靠的质量估计 + 客观反馈**。把这条线做扎实,模型随便换、随便加,产品的能力上限只取决于编排有多聪明,不取决于任何一家模型有多强。

*本计划基于经对抗验证的学术证据 + 本团队一手横评实测;所有量化收益为最佳情形上限,落地收益取决于任务分布与估计器质量。仅规划,启动需拍板。*
`;

(async () => {
  const dir = join(process.cwd(), 'output', 'AIOS-优化计划');
  mkdirSync(dir, { recursive: true });
  const path = await writeDeliverable(md, 'AIOS核心能力优化计划.pdf', dir);
  console.log('✓ 优化计划 PDF 已生成 →', path);
})();
