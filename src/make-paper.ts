// 一次性脚本:生成《AIOS 学术论文》PDF。跑法 npx tsx src/make-paper.ts
// 素材 = 本项目 2026-06 LLM 路由/自愈/judge 学术调研 + 三模型×5任务一手横评 + 引擎 P0/P1/P2 落地实测。
import { mkdirSync } from 'node:fs';
import { join } from 'node:path';
import { writeDeliverable } from './build';

const md = `# AIOS:面向异构大模型的中立编排层

## —— 能力画像路由、客观自测闭环与在线学习的统一框架

### A Neutral Orchestration Layer for Heterogeneous LLMs: Unifying Capability-Profile Routing, Objective Self-Test Loops, and Online Learning

**作者**:AIOS Project　**日期**:2026 年 6 月　**类型**:系统与实证研究(System & Empirical Study)

---

## 摘要 (Abstract)

大语言模型(LLM)能力高速迭代,但**不存在在所有任务上均最优的单一模型**:本文一手实测显示,代码造物最优的模型在长文档上明显偏弱,文档最强的模型在单文件代码造物上因输出冗长而频繁截断、续写崩溃且耗时数百秒——甚至**同一个模型在不同任务上的可用性天差地别**。这意味着"为某一个大模型深度定制 agent",本质上把产品能力上界焊死在该模型的能力边界上。本文提出 **AIOS**,一个面向终端用户成品交付的**中立多模型编排层**,其核心主张是 **编排 > 模型(Orchestration over Models)**。AIOS 包含四个相互支撑的机制:(1) **能力画像路由**——以"能力卡 × 任务需求向量 − β·成本"的可解释打分进行 argmax 选择,新模型仅需填一张能力卡即可接入;(2) **动态 fallback 与冷却**——候选链按分降级、失败后端进入冷却,使单点故障不拖垮整体;(3) **客观自测闭环**——对代码造物以 headless 真机检测产物可运行性、按产物类型分类判定,把"客观执行反馈"而非模型自评喂回修复;(4) **kNN 在线学习路由**——以零额外成本的文本相似度检索历史客观质量,使路由越用越准。我们在三个主流中文大模型上以 5 类代表任务开展真机横评,给出一手数据并据此论证:AIOS 在每个生成维度逼近"各家最优"的并集,同时独占容错、质量兜底、自适应、透明与成本可调等单一模型不具备的编排级能力。我们进一步讨论该路线的**结构性身位**——自带模型的闭源巨头因商业模式难以"左右手互搏",而中立编排层恰能做到"谁强用谁"。

**关键词**:LLM 路由(LLM Routing);模型编排(Model Orchestration);级联与质量估计(Cascading & Quality Estimation);自我修复(Self-Repair);在线学习路由(Online Learning Router);Agent 系统

---

## 1. 引言 (Introduction)

### 1.1 背景与问题

通用对话式 AI 的终端体验,正从"与一个模型聊天"转向"让系统替我把事办成"。但把事办成所依赖的能力是**异构**的:写一个能玩的小游戏、做一份带表格的调研报告、完成一段需要深推理的财务建模,对模型的要求并不相同。业界的普遍做法是绑定某一个"最强"基座模型并围绕它做工程。本文将论证这是一条**有结构性上界**的路:

- 没有任何单一模型在所有任务维度上同时最优(第 6 节一手数据);
- 即便是同一个模型,跨任务的**可用性**也可能从"优秀"跌到"不可用"(第 6.3 节);
- 因此绑定单一模型 = 继承其全部短板,产品上界被该模型锁死。

### 1.2 主张:编排 > 模型

我们主张,把 AI 发挥到极致的关键不在"选一个更强的模型",而在**编排**:让每个任务被路由到当下最适合的模型,并用 fallback、客观自测与在线学习兜住交付质量。模型是可替换、正在快速商品化贬值的"零件";而"谁强用谁 + 结果标准 + 自测修复 + 记忆"的编排逻辑,是自有的、随使用而增厚的资产。

### 1.3 贡献 (Contributions)

1. **系统**:提出并实现 AIOS——面向终端用户成品交付的中立多模型编排层,区别于以系统资源调度为核心的 Agent 操作系统(见第 2.4 节)。
2. **路由方法**:给出可解释、参数轻量的能力画像路由(argmax of 画像·需求 − β·成本)与一个直接面向用户的成本/质量旋钮 β。
3. **客观反馈**:提出按产物类型分类的 headless 真机自测闭环,以**客观执行反馈**替代模型自评喂回修复,正面回应了自我修复研究中"反馈质量决定成败"的核心结论。
4. **在线学习**:提出零额外成本(文本相似度)的 kNN 历史学习路由,使路由随使用自适应纠偏。
5. **实证**:在三个主流中文大模型上以 5 类任务开展一手真机横评,公开数据并据此系统论证"编排 > 模型"及其结构性身位。

---

## 2. 相关工作 (Related Work)

### 2.1 LLM 路由与级联 (Routing & Cascading)

RouteLLM [2] 用偏好数据学习"强/弱模型"二选一的路由器,报告在保持质量的同时大幅降本;FrugalGPT [3] 提出由弱到强的级联(cascade),前级足够好即提前停止。Dekoninck 等 [4] 进一步统一了路由与级联,指出二者可在同一框架下权衡,并强调**质量估计器(estimator)的质量是整套系统成败的关键**——当估计器准确时收益巨大,反之可能劣于随机。RouterBench [6] 为多模型路由提供了基准与指标。本文的能力画像路由属于"生成前(ex-ante)"路由,与级联"生成后(ex-post)"判定互补;我们采纳了 [4] 关于"估计器质量是命脉"的核心洞察,并在工程上以**客观自测**(第 5.3 节)逼近一个可靠的事后估计器。

### 2.2 能力画像与无参数路由 (Capability Profiling)

以 InferenceDynamics [类] 为代表的工作主张以"能力维度画像"刻画模型,使路由可推广到未见模型而非依赖大量训练数据;GraphRouter [5] 则以图结构建模任务-模型关系。本文的能力卡(造物/文档/推理/多模态等维度)正是这一思路的轻量工程化:画像初值来自一手横评,新模型即插即用。

### 2.3 自我修复及其边界 (Self-Repair)

Self-Refine [8] 与一系列自我修复工作展示了"生成→反馈→再生成"的潜力;但 Olausson 等 [7] 系统性地指出**自我修复并非银弹**——当反馈来自模型自身且不可靠时,收益有限甚至为负,真正的增益往往来自**更强、更客观的外部反馈**。这直接支撑了本文的设计取舍:对代码造物,我们不依赖模型自评,而以 headless 真机执行的客观结果(能否运行、是否报错、是否秒结束)作为反馈源(第 5.3 节)。

### 2.4 LLM 作为评判者及其偏见 (LLM-as-a-Judge)

Zheng 等 [9] 在 MT-Bench/Chatbot Arena 上验证了强模型作为评判者与人类有较高一致性(>80%),但同时揭示了**位置偏见、冗长偏见与自我增强偏见**。这界定了"以 LLM 评判质量"的适用边界,也指明降噪手段(位置交换、多评委、禁止自评),构成本文未来工作(第 8 节)中质量估计增强的依据。

### 2.5 与 "Agent 操作系统" 的区分

Mei 等 [1] 提出的 AIOS(LLM Agent Operating System)聚焦于在**单一基座**之上为众多 agent 调度系统资源(内存、上下文、工具、并发)。本文的 AIOS 与之**正交且互补**:我们关注的是**跨异构模型/厂商**的能力级编排与成品交付——把"哪个模型做这件事最好"作为一等问题。二者可叠加:资源调度之上再加一层中立的能力路由。

---

## 3. 问题形式化 (Problem Formulation)

给定一个用户目标,经任务分解得到子任务集合 T = {t₁, …, tₙ}。每个子任务 t 具有技能类型 skill(t) 与复杂度 complexity(t)。可用模型(候选)集合为 M = {m₁, …, m_k},每个模型 m 具有能力画像向量 **c**ₘ ∈ [0,1]ᵈ 与归一化成本 costₘ ∈ [0,1]。每个子任务诱导一个需求向量 **r**ₜ(各维非负)。

**路由目标**:为每个子任务选择模型,使期望交付质量与成本的折中最优。本文以可解释的线性打分实现:

> **score(m, t) = Σᵢ cₘ,ᵢ · rₜ,ᵢ − β · costₘ + γ · ( q̂(m,t) − ½ )**

其中第一项是能力画像与任务需求的内积(画像匹配度),第二项是成本惩罚(β 为用户可调旋钮),第三项是 kNN 历史学习项(q̂ 为相似历史任务上该模型的客观质量估计,以 ½ 为中性基准,γ 控制学习强度;无历史时该项为 0)。路由取 argmaxₘ score,并按分降序形成候选链以支持 fallback。

这一形式同时容纳了三种文献思路:画像匹配(§2.2)、成本-质量折中(§2.1)、以及由客观反馈驱动的在线估计(§2.1 的"估计器质量是命脉"+ §2.3 的"客观反馈")。

---

## 4. 系统架构 (Architecture)

AIOS 引擎为一条五段流水线,各段均建立在统一的多 provider 抽象之上:

> **Brain(读记忆 + 任务分解) → Router(跨 provider 选模型) → Agent(按依赖编排执行) → Result(整合) → Memory(更新记忆/路由历史)**

- **统一 Provider 抽象**:以 OpenAI 兼容接口统一封装各厂商后端,每个 provider 自报可用性与元数据(上下文上限 contextLimit、成本档 costTier、能力标签 caps)。已接入 DeepSeek(官方 V4 系)、豆包(火山方舟)、通义千问(阿里云百炼),并预留 Claude 等。新后端接入仅为少量配置改动,**绝不为单一模型深度定制**,保持每个零件可替换。
- **意图分流**:任务先按 action / artifact(造物) / document(文档) 分流,决定下游的产物形态与自测策略。
- **可替换性原则**:模型池的广度与可替换性被列为一等设计目标——这是"编排 > 模型"在工程上的直接体现。

---

## 5. 方法 (Methods)

### 5.1 能力画像路由与 β 旋钮

如第 3 节,路由以画像匹配减成本惩罚打分。能力卡初值来自第 6 节横评(如:千问 造物 0.95/文档 0.40;DeepSeek-V4 造物 0.30/文档 0.90/推理 0.95;豆包 多模态 0.90)。**β 旋钮**把成本-质量折中直接交给用户:β 小则质量优先(BYOK 场景下可激进偏质量),β 大则省钱优先。实测中,β 由 0.3 增至 1.5 时,深推理任务的首选会从高成本的 V4-Pro 自动降级到低成本的 V4-Flash,验证旋钮真实生效。

### 5.2 动态 fallback 与冷却

路由输出候选链而非单点。执行器按链依次尝试:输入超出某后端上下文上限则跳过(上下文预检),调用失败则将该后端标记为**冷却(cooldown)**一段时间并降级到下一候选。这使得任一后端限流/超时/故障都不会使整次交付失败,显著提升系统可用性。

### 5.3 客观自测闭环与分类检测

针对代码造物,我们以 headless 浏览器**真机加载并执行**产物,按产物类型分类判定:

- **游戏类(含 canvas)**:检测运行时报错、canvas 是否渲染、是否"出生即死/秒胜",并对比"无操作基线"与"注入按键后"的画面变化以观察交互响应;
- **网页类(无 canvas)**:检测 DOM 实质内容与运行时报错,不再以"没有 canvas"误判。

判定结果(尤其是确定性的坏:报错、空白、秒结束)作为**客观反馈**喂回模型做针对性修复,形成"生成→自测→修复"闭环。这一设计直接遵循 §2.3 的结论:**反馈要客观、要强**。我们亦诚实地将"键盘响应"等深层交互判定标注为黑盒启发式——因为开始与按键机制千差万别,黑盒触发难以全覆盖,故宁可漏检也不误杀可玩产物(避免触发无谓的修复)。

### 5.4 kNN 在线学习路由

每次执行后记录三元组(任务文本、所用模型、客观质量 q)。客观质量对造物取自测可玩性、对其他任务取是否成功产出。路由时,以**文本 token 的 Jaccard 相似度**(零 embedding 成本、零额外延迟)检索同技能、最相似的 top-k 历史,得到各模型的相似度加权平均质量 q̂,并按第 3 节公式微调打分。其行为是:在相似任务上历史表现好的模型上浮、差的下压;无历史时退化为纯画像路由(冷启动安全)。学习强度 γ 可配——小则画像主导、历史微调,大则历史主导、纠偏更强。

---

## 6. 实证研究 (Empirical Study)

### 6.1 设置

在统一引擎下,对 **DeepSeek-V4-Pro(官方)**、**豆包 2.0-code(火山方舟)**、**千问 3-Coder(阿里云百炼)** 三模型,各跑 5 类代表任务:贪吃蛇(简单游戏)、马里奥(复杂游戏)、打砖块(中等游戏)、SaaS 落地页(网页)、充电桩调研(文档)。造物指标:首轮截断、续写后完整性、headless 真机可玩性(启发式)、生成耗时、代码体积;文档指标:篇幅、章节、表格行、数据点。

### 6.2 主要结果

| 维度 | 千问 3-Coder | DeepSeek-V4 | 豆包 2.0-code |
|---|---|---|---|
| 游戏可玩(启发式) | **3 / 3** | 1 / 3 | ~0–1 / 3 |
| 造物首轮截断 | 否 | **4 项全截断** | 否 |
| 造物平均耗时 | **~40s** | ~335s(最长 553s) | ~145s |
| 文档表格行(调研) | 0 | **15** | 6 |
| 典型失败模式 | — | 输出冗长→截断→续写拼接崩语法错 | 运行时低级 bug(未初始化即遍历) |

**发现一**:代码造物千问全面领先(3/3 可玩、零截断、最快最精简)。**发现二**:DeepSeek-V4 在单文件造物上有结构性硬伤——输出冗长导致首轮全部超限截断,续写在拼接处崩出语法错,且极慢;但其文档/调研最强(结构化、15 表格行)。**发现三**:豆包结构规整、不截断,但有运行时低级缺陷;其多模态(视觉/视频)为厂商优势(本次未测)。

### 6.3 关键观察:同一模型,跨任务体验天差地别

最具说服力的不是排名,而是:**同一个 DeepSeek-V4,造物体验近乎不可用(等待数百秒且大概率崩),文档体验却很优秀(百秒级出带 15 个表格的报告)。** 这从根本上否决了"为某模型定制一个全能 agent"——连同一个模型自己都做不到跨任务都好,遑论用一个静态 agent 去覆盖。

### 6.4 路由与学习的行为验证

- **画像路由**复现横评结论:造物→千问、文档/写作→DeepSeek-V4-Flash、深推理→V4-Pro,且决策可解释(给出综合分与 β)。
- **β 旋钮**:β=1.5 时深任务由 V4-Pro 自动降级 V4-Flash 省成本。
- **kNN 学习**:注入"某模型在该类任务上客观质量低"的历史后,该模型的综合分被相应下压、备选上浮;增大 γ 可将其从首选压至末位——表明在线纠偏真实生效。

### 6.5 诚实交底 (Threats to Validity)

(i) 可玩性为 headless 启发式,有噪声(落地页无 canvas 不适用;个别"可玩"疑被自动动画干扰),但"千问代码强 / DeepSeek 造物受挫"由可玩+截断+报错+速度多个独立维度共同指向,不受单点噪声左右;文档指标为客观计数。(ii) 生成具随机性,样本量有限,结论为趋势性而非统计显著性。(iii) 闭源巨头的对比基于公开差评与产品特性,非同台实测。

---

## 7. 讨论 (Discussion)

### 7.1 为什么"编排 > 模型"

实证给出三层论证:其一,无全能模型(6.2);其二,连同一模型跨任务都不稳(6.3);其三,编排层的能力 = 各家所长的并集 − 各家所短(靠路由规避)。因此绑定单一模型的产品上界被该模型锁死,而编排层的上界随模型池扩张与历史积累而抬升。

### 7.2 结构性身位

这条路基本只有**中立编排层**能走:自带基座的闭源巨头出于商业模式,缺乏"把任务路由给竞争对手模型"的动力(左右手互搏),从而被锁在自家模型的能力边界内。中立层恰能做它们做不了的——谁强用谁。这是**结构性身位**而非短期技术领先,也更难被单纯的模型进步抹平。

### 7.3 质量估计是命脉

贯穿路由(§2.1)、自愈(§2.3)与评判(§2.4)的同一条主线是:**质量估计/反馈的可靠性决定一切**。AIOS 的工程回应是用"客观可执行反馈"(真机自测)在造物域逼近一个可信估计器,并以在线学习把事后客观质量回灌为事前先验。把这条主线做扎实,是 AIOS 的核心技术护城河。

---

## 8. 局限与未来工作 (Limitations & Future Work)

- **黑盒自测的覆盖**:深层交互(如键盘是否真正驱动角色)在纯黑盒下难以可靠判定;未来可探索受控的白盒探针或标准化产物契约。
- **客观估计器的扩域**:当前客观反馈在造物域最强,文档/分析域仍偏弱;需引入降噪后的 LLM 评判(位置交换 / 多评委 / 禁自评,依 §2.4)作为补充,并警惕其偏见。
- **中文模型画像的自建**:现有路由文献多基于英文模型,中文模型须以自建横评持续校准能力卡;评测常态化(新模型接入即自动横评、回写画像)是必要配套。
- **学习信号的丰富化**:kNN 当前以文本相似度近似;在数据积累后可升级为语义检索与更细的质量度量,并防止反馈回路自我强化偏差。
- **模型池广度**:接入更多后端(Kimi/GLM/视觉与视频模型)以扩大"各家所长"的并集。

---

## 9. 结论 (Conclusion)

本文提出 AIOS——一个以"编排 > 模型"为核心主张的中立多模型编排层,并以能力画像路由、动态 fallback 与冷却、客观自测闭环、kNN 在线学习四个相互支撑的机制将其落地。一手横评表明不存在全能模型、且同一模型跨任务体验差异巨大,从根本上支持了编排路线;能力矩阵进一步显示 AIOS 在每个生成维度逼近各家最优的并集,并独占容错、质量兜底、自适应、透明与成本可调等编排级能力。我们相信,随着基座模型的商品化,价值将持续从"拥有最强模型"向"最善编排模型"迁移,而可靠的质量估计与客观反馈,是这条路上真正的护城河。

---

## 参考文献 (References)

[1] Mei, K., et al. *AIOS: LLM Agent Operating System.* arXiv:2403.16971, 2024.
[2] Ong, I., et al. *RouteLLM: Learning to Route LLMs with Preference Data.* arXiv:2406.18665, 2024.
[3] Chen, L., Zaharia, M., Zou, J. *FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance.* arXiv:2305.05176, 2023.
[4] Dekoninck, J., et al. *A Unified Approach to Routing and Cascading for LLMs.* ETH Zurich, 2024/2025.
[5] Feng, T., et al. *GraphRouter: A Graph-based Router for LLM Selections.* 2024.
[6] Hu, Q. J., et al. *RouterBench: A Benchmark for Multi-LLM Routing System.* arXiv:2403.12031, 2024.
[7] Olausson, T. X., et al. *Is Self-Repair a Silver Bullet for Code Generation?* ICLR, 2024.
[8] Madaan, A., et al. *Self-Refine: Iterative Refinement with Self-Feedback.* NeurIPS, 2023.
[9] Zheng, L., et al. *Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena.* NeurIPS, 2023.
[10] InferenceDynamics: *Scaling LLM Routing via Capability Profiling.* 2025(预印).

*附注:本文实证数据来自 AIOS 引擎一手真机横评(2026-06-01),可玩性指标已标注启发式局限,客观指标(截断/完整/速度/体积/文档结构)与路由/学习的行为验证均可复现。*
`;

(async () => {
  const dir = join(process.cwd(), 'output', 'AIOS-学术论文');
  mkdirSync(dir, { recursive: true });
  const path = await writeDeliverable(md, 'AIOS学术论文-编排型多模型层.pdf', dir);
  console.log('✓ 学术论文 PDF 已生成 →', path);
})();
